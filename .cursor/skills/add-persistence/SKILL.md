# Add persistence
Reuse existing JSON/settings patterns; define a versioned schema; failure-safe writes; per-character separation; malformed/missing/old-file handling; no per-frame writes; debounce/batch; deterministic serialization; parser/migration tests; document storage/privacy. Quest history is append-only. Contract exporters must use mirrored Contract identity/SHA-256 rules (`producer.version` is metadata only).
