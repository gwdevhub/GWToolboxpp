# Add Toolbox window
Find a comparable window; follow singleton/lifecycle/registration/settings patterns; keep Draw presentation-only; move observation/persistence elsewhere; unregister callbacks and release resources; document upstream registration files; build and list runtime checks.
