--- [major]
C:/Users/m/source/GWToolboxpp/GWToolboxdll/Widgets/CartographerWidget.cpp:841 ? ResolveGoal still has a path to the RAW FOG POINT, and it is reachable on a data failure, not just by design.\n\n    if (!(GetCartoGrid(grid) && !grid.IsExplored(fx, fy))) return GoalKind::Waypoint;\n\nThis conflates "grid unavailable" with "tile already explored". AddCustomPointImpl (line 905) calls RefreshCustomTargetStand outside Update's GetCartoGrid guard, so a transient GW::GetWorldContext() / zero-extent read turns a genuine fog point into GoalKind::Waypoint. TargetGoal() (line 779) then returns &target.wm ? the raw fog point ? the quest marker is planted on the fog, and Update's Waypoint branch (line 1678) fires RemoveCustomPointAt(target.wm) once the player gets within 2 wm units, silently DELETING the user's point without ever crediting it. CustomPoint::was_fog already holds the authoritative answer

--- [major]
C:/Users/m/source/GWToolboxpp/GWToolboxdll/Widgets/CartographerWidget.cpp:1774 and :1152 ? deleting target.on_map outright makes a fog point on ANOTHER MAP indistinguishable from an unreachable one, and the widget now tells the user to throw it away.\n\ncustom_points is a global, persisted, cross-map list (SerializePoints/ParsePoints store bare world-map coords). WorldMapWidget::WorldMapToGamePos is a pure affine transform that always succeeds, so for a point stored on a different map every ring cell converts to coordinates the current instance's pathmap does not cover, IsPositionWalkable fails for all 36 samples of all 9 (or 49) cells, ResolveStandWorldPos returns false, and ResolveGoal returns GoalKind::None. The selection loop sets blocked_point = true and skips it, and BuildStatusText reports no_goal = "no reachable square credits your fog point - right-click it on the map to remove 

--- [major]
C:/Users/m/source/GWToolboxpp/GWToolboxdll/Widgets/CartographerWidget.cpp:1774 ? the new selection loop probes without a budget, contradicting the design the file already establishes.\n\nPreviously ResolveStandWorldPos ran once per scan, for the single selected custom point. Now ResolveGoal is called for EVERY custom point in distance order until one resolves, and every point that resolves to None has to exhaust its whole ring first ? 9 cells at normal range, 49 with a BEC ? at kStandSamples^2 = 36 IsPositionWalkable BSP descents per cell (plus a heap-allocating CopyBlockedPlanes inside IsPositionReachable for each walkable sample). That is up to ~1800 pathing queries per unresolvable point, in one Update tick, on the game thread. SweepStandCells deliberately caps itself at budget = 6 cells per scan for exactly this cost, so the new path can do ~8x that per point with no cap at all.\n\nI

--- [minor]
C:/Users/m/source/GWToolboxpp/GWToolboxdll/Widgets/CartographerWidget.cpp:1796 ? StandRoutable is applied to SUGGESTION cells, where its own stated justification does not hold, and it rejects them silently.\n\nThe comment on StandRoutable (line 316) grounds the guard in QuestModule::SetCustomQuestMarker degrading to a travel marker ? but SyncQuestMarker returns early for !target.custom, so suggestions never place a marker and the rationale does not apply. Its real job here is filtering the out-of-rect cells that ResolveStandWorldPos injects into probe->cells on demand, which is a different concern. Meanwhile the fog-point path logs warned_stand_off_rect when the guard bites, and this one logs nothing. If GetMapWorldMapBounds is even slightly tighter than the map's real geometry ? the exact uncertainty the one-shot diagnostic was added to measure ? legitimate edge stand cells disappear wi

--- [minor]
C:/Users/m/source/GWToolboxpp/GWToolboxdll/Widgets/CartographerWidget.cpp:567 ? warned_stand_off_rect is never reset, unlike every other warned_* flag. ResetState (line 861) clears warned_no_data and warned_no_fog on map change; this one is not in the list and is not reset when EnsureMapRect refreshes either. Since the whole point of the log line is to measure how often the rectangle guard is too tight, a single fire for the entire session hides whether it bites on any subsequent map. Reset it per map alongside the others.

--- [minor]
C:/Users/m/source/GWToolboxpp/GWToolboxdll/Widgets/CartographerWidget.cpp:1799 ? the hysteresis now compares two different points. Suggestion cand_d2 is still measured to the cell CENTRE (const float d2 = Dist2(centre, player_wm);) while cand.wm ? and therefore target.wm on the next scan ? is now the probed FOOTING (cand = {true, false, cell.first, cell.second, sc.reveals, stand}). Line 1826's `cand_d2 >= 0.7f * Dist2(target.wm, player_wm)` therefore weighs a centre distance against a footing distance, which can differ by up to kStandSampleOffsetMax (~19 wm units, 0.6 of a cell) ? enough to flip the 0.7 threshold on a nearby cell and make the target ping between two squares. Measure cand_d2 from stand too.

--- [minor]
C:/Users/m/source/GWToolboxpp/GWToolboxdll/Widgets/CartographerWidget.cpp:1767 ? blocked_point goes stale on Update's early-return paths. It is only recomputed once execution reaches the selection loop, but Update returns before that on !GetCartoGrid(grid) (line 1621), !GamePosToWorldMap(player->pos, ...) (line 1630), the 1s scan throttle and the 2s map-settle guard. RemoveCustomPointNear (line 2037) and ClearCustomPoints do not clear the flag either, so after the user follows the status line's own instruction and right-clicks the point away, the overlay can keep telling them "no reachable square credits your fog point - right-click it on the map to remove it" for a point that no longer exists. Clear it in RemoveCustomPointNear/ClearCustomPoints (it is already cleared in ResetState).

--- [blocker]
REGRESSION - cross-map fog points are now permanently dead and the UI tells the user to delete them. Removing `target.on_map` means `ResolveGoal` (line 841) runs `ResolveStandWorldPos` on an off-map wm; every probe sample goes through `WorldMapWidget::WorldMapToGamePos` using the CURRENT map's anchor, so nothing is walkable/reachable and it always returns `GoalKind::None`. Consequences: (a) the selection loop (line 1779) skips the point forever and latches `blocked_point = true`; (b) `SyncQuestMarker` gets a null goal and calls `ReleaseQuestMarker()`; (c) `BuildStatusText` prints "no reachable square credits your fog point - right-click it on the map to remove it", and every subsequent suggestion line permanently carries " (a fog point here credits nothing reachable)" (line 1185). Before the diff this case set `on_map=false`, kept the point as the target, and passed the raw wm to `QuestM

--- [major]
`StandRoutable` is a quest-marker-only constraint but it is applied to routing, arrival, the overlay, and to suggestions that never get a marker. In the suggestion loop (line 1796) `if (!WorldMapWidget::GamePosToWorldMap(sc.pos, stand) || !StandRoutable(stand)) continue;` silently drops reachable, high-value stand cells from scoring - yet `SyncQuestMarker` (line 809) returns early for `!target.custom`, so a suggestion never places a marker at all. The filter is pure loss there, with no log and no user-visible reason. Inside `ResolveStandWorldPos` (line 605) the same filter suppresses the crediting square from the target, the pulsing overlay rect (line 1495) and the arrival test (line 1692) - and it still applies when the user has turned OFF "Set a quest marker to fog points" (`set_quest_marker`, line 1878), where no marker is ever placed. It also mis-states the outcome: `StandRoutable` m

--- [minor]
Hysteresis now compares two different distances for suggestions. `cand_d2` is `Dist2(centre, player_wm)` (line 1798) but `cand.wm` is now the probed footing `stand` (line 1804), and line 1827 tests `cand_d2 >= 0.7f * Dist2(target.wm, player_wm)` where `target.wm` is a footing. A footing can sit up to `kStandSampleOffsetMax` (~18.9 wm units, over half a tile) from its cell centre, so the incumbent and the challenger are measured on different scales and the target can be kept or dropped for the wrong reason - visible as suggestion churn on coastline cells, exactly the cells the footing change was made for. Score candidates on `stand` too, or keep `wm` as the centre and carry the footing in a separate field (as the custom path already does with `stand_wm`).

--- [minor]
BEC (r=3) is now strictly a fallback for fog-point routing, and the demotion comment is stale. `ResolveStandWorldPos` lines 623-624 return the near ring whenever it yields anything, so with a Bird's Eye Compass the wide ring is only consulted when all 9 near cells are unreachable or unroutable - the "walk less with a BEC" benefit is gone for fog points (suggestion scoring via `ScoreStandCell` still uses `RevealRadius()`, so it survives there). Before the diff near and wide candidates were sorted together and the nearest won, so a wide cell up to 2 tiles closer could be chosen. The tradeoff is defensible and documented at 621-622, but it should be a deliberate call. Knock-on: the comment at lines 1749-1750 ("demote it so the next resolve sends them closer in") is now false - a near stand was already tried and rejected, so inserting the fog cell into `probe->strict` makes `CellQualifies` f

--- [minor]
`ResolveGoal` (line 845) conflates "cartography grid unavailable" with "already explored": `if (!(GetCartoGrid(grid) && !grid.IsExplored(fx, fy))) return GoalKind::Waypoint;`. A Waypoint routes to the raw fog point and, on arrival within 2 units, silently deletes the point (lines 1678-1684) - the exact behaviour this rewrite exists to eliminate. `Update` guards it (early return at line 1622), but `AddCustomPointImpl` -> `RefreshCustomTargetStand` (line 919) does not. Return None (or leave the target unresolved) when the grid is missing.

--- [minor]
Per-scan cost and probe-cache pollution. `ResolveGoal` now runs for every custom point nearer than the first resolvable one, every 1s scan, and the winning point is then resolved a *second* time by `RefreshCustomTargetStand` (lines 1835/1841) with the results of the first resolve (`goal_wm`, `goal_cell`) thrown away at line 1777-1778. For an unresolvable point `try_ring`'s early-break never fires (it is gated on `found`), so both passes walk all 9 and then all 49 candidates, calling `ProbeStandCell` (36 walkable+reachable tests each) on far-away cells and inserting them into `probe->cells` keyed by coordinates from another map - each insert sets `coverage_stale`, forcing a full `RecomputeCoverage` (rescore of every probed cell + full `RebuildFog`) on the next scan. It self-limits via the cache, but `DropProbeIfGatesMoved` clears `probe->cells` and the whole thing repeats.

--- [minor]
AGENTS.md rule #1 (zero comments restating the code) - three new comments restate rather than explain: line 530-531 "The half-cell (s+0.5)/kStandSamples offsets keep every sample clear of the cell edges" restates the `(sx + 0.5f) / kStandSamples` expression three lines below; lines 743-744 on `enum class GoalKind` restate the three enumerator names; lines 839-840 on `ResolveGoal` restate its two-line body. Separately, the `warned_no_fog` log at line 1811 ("no fogged walkable ground left on this map") now also fires when the only reason there is no candidate is a blocked custom point, which makes the log misleading.
